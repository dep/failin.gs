<% form_for @password_reset, url: password_path, remote: true do |f| %>
  <fieldset>
    <%= f.error_messages %>
    <%= f.label :email %>
    <%= f.text_field :email, type: "email" %>
    <%= f.submit %>
  </fieldset>
<% end %>
